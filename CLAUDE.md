# field-note-cad

## コメント方針

### ドキュメンテーションコメント
関数・型・インターフェースには `/** ... */` 形式のコメントを付ける。

```ts
/** 図面データのJSON文字列をパースして型安全なオブジェクトを返す */
export function parseDrawingJson(rawText: string): JsonParseResult { ... }
```

- `@param` / `@returns` は引数・戻り値が名前から自明でないときのみ追加する
- interface/type の非自明なフィールドには1行説明をインラインで付ける

### インラインコメント
複雑な処理・非自明な制約・回避策には `//` を1行添える。

```ts
// SVGはy軸が下向きなので、CAD座標（y上向き）を符号反転する
```

- 「何をしているか」ではなく「なぜそうなっているか」を書く
- 自明なことは書かない

## コミット方針

ユーザーから明示的に「コミットして」と指示があるまで `git commit` を実行しない。
実装が完了しても、コミットせずに作業を終える。
